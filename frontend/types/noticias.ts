/**
 * Contratos TS da feature notícias do município
 * Dashboard Financeiro - Bandeirantes MS
 */

export interface NoticiaResponse {
  titulo: string;
  chamada: string;
  link: string;
  data_publicacao: string;
  fonte: string;
}
